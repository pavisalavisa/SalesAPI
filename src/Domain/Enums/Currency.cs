namespace Domain.Enums
{
    // It might be valuable to model currency in a different manner but for the sake
    // of simplicity let it be enum
    public enum Currency
    {
        Euro = 1
    }
}
